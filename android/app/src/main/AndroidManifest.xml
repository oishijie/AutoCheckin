<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 关于页「检查更新」：读 GitHub Releases 的版本号 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 唤醒屏幕 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 开机后重排定时 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 精确闹钟（Android 12+） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!--
        「显示在其他应用上层」（悬浮窗） —— 定时签到能跑起来的关键。

        Android 12+ 起，处于后台的应用调用 startActivity() 会被系统静默丢弃：
            W ActivityTaskManager: activity start fail: Background activity start
              callingPackage: com.xiaoyao.autocheckin
              isCallingUidForeground: false
              allowBackgroundActivityStart: false

        定时签到（AlarmReceiver → runAsync）触发时本 App 就在后台，
        于是 deeplink 全部被拦、rootInActiveWindow 变成 null，
        表现为「日志已发起跳转，但页面根本没动，然后在 4 轮重试里空转」。

        持有 SYSTEM_ALERT_WINDOW 是 Android 官方的后台启动豁免条件之一，
        授权入口：设置 → 应用 → 本应用 → 显示在其他应用上层。
        手动测试（App 停在前台）不受此限。
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Android 11+ 包可见性：允许按包名判定前台 App -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoCheckin">

        <!-- singleTop：配合 onNewIntent，让带 runNow 参数的启动指令
             在 Activity 已存在时也能触发（不必 force-stop，避免踢掉无障碍服务） -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 无障碍服务：由系统绑定，必须声明 BIND_ACCESSIBILITY_SERVICE 权限 -->
        <service
            android:name=".CheckinAccessibilityService"
            android:exported="true"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
