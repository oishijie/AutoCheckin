# ============================================================
#  构建产物
# ============================================================
# APK 不进版本库：每次构建都会变、无法 diff，只会让仓库持续膨胀。
# 分发走 GitHub Release，源码仓保持纯净。
*.apk
builds/

# Android 工程的忽略规则见 android/.gitignore（gradle 缓存、local.properties 等）

# ============================================================
#  调试工作区
# ============================================================
# refs/ 是侦察期产物：uiautomator 控件树 dump、临时截图、deeplink 抓包原文。
# 体积 11MB，且含个人签到凭证（qrcode 串），不随项目分发。
refs/

# ============================================================
#  本地留痕
# ============================================================
.workbuddy/
*.log

# 系统文件
.DS_Store
Thumbs.db
